node_modules/
.env
dist/
.DS_Store
.firebase/

functions/.env
*.log
# --- Local tooling & debug artifacts ---
*.bak
*.bak.*
# One-off debug/patch scripts stay ignored by default — this repo accumulates
# them fast (50+ as of 2026-08). The handful that are actual reusable ops
# tooling, not incident scratch, are tracked explicitly below.
*.ps1
!deploy.ps1
!deploy-functions.ps1
!bump-version.ps1
!check-firebase-project.ps1
.claude/
admin/_force_admin_backup/
admin/_gate_backup/
web/src/components/*.bak*
temp_*
# Windows reserved device names
nul

# Service account keys and runtime secrets — NEVER commit these
functions/serviceAccountKey.json
functions/exam-coach-ai-platform-firebase-adminsdk-fbsvc-ae6b0d59c6.json
functions/.runtimeconfig.json

functions/*.json
!functions/package.json
!functions/package-lock.json
!functions/tsconfig.json
functions/.runtimeconfig.json
*.env
.env*
firebase-admin*.json

# Bloom's classification pipeline outputs — contain proprietary question content
functions/blooms-*.csv
functions/.blooms-progress.json


# added by github-push batch
.venv/
venv/
__pycache__/
*.pyc
.env.local
.env.*.local
secrets.*
*.key
*.pem
credentials*
build/
.next/
Thumbs.db
